# CC MCP LSP Java — 当前进展

## 已完成

- **管理面板**：侧边栏视图，显示监听地址、活跃会话、连接历史、重启日志，支持启动/停止/重启/端口变更
- **MCP 接口说明**：VS Code 原生视图分组 + 编辑器标签页完整文档（架构、LSP 映射、工具参考、使用场景、限制提醒）
- **Java 查询测试**：侧边栏输入 + 编辑器结果面板，支持搜索类型和获取源码；结果面板含过滤（种类/来源/名称）、排序、文件跳转
- **Server 状态事件**：`onDidChangeStatus` + `getServerInfo()`，自动检测本机 IP
- **连接/重启追踪**：会话建立/关闭时间记录，重启历史日志
- **GitHub skills**：从 cc-kit 导入 github-repo、git-workflow
- **注释保护**已落地
- **全量 Webview React 19 迁移**：所有面板从内联 HTML 模板迁移至 React 19 + TypeScript + Vite 构建
  - `src/webview/` 下 4 个独立 React 应用 + 共享模块
  - 构建分离：tsup (扩展) + Vite (webview)，输出 `dist/` + `dist-webview/`
  - 数据传递改为 postMessage 事件驱动，asset 路径自动通过 `webview.asWebviewUri()` 解析

## 待办

- 连接历史持久化（当前仅内存，重启后丢失）
- LSP 方法扩展：跳转定义、查找引用、悬停提示、代码补全（规划中）

## 近期变更（2026-05-27）

- **调用图分析面板增强**：从简单状态点+查询框升级为完整信息页面
  - 四格状态卡片：侧车状态、扫描状态、数据库大小、Classpath 输入路径
  - 分析日志面板：实时滚动显示扫描/查询/清理过程
  - 状态轮询：每 5 秒自动刷新侧车状态，修复了"刷新按钮被禁用"的死锁 bug
  - 方法列表查询：新增加"方法列表"快速按钮
  - 操作说明区域：引导首次使用
